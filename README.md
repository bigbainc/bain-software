Note that all python packages must be installed locally as per the requirements.txt file.

The following credentials can be used to login as well as access the admin panel to manually add data if desired.
Username: admin
Password: admin